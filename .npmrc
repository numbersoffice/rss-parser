# A flat node_modules makes `COPY --from=deps /app/node_modules` in the
# Dockerfile trivially correct — pnpm's default symlink-into-.pnpm layout only
# survives the copy if the whole store tree comes along.
node-linker=hoisted
