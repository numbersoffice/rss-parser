DATABASE_URL=file:./rss-parser.db
PAYLOAD_SECRET=YOUR_SECRET_HERE
PAYLOAD_PUBLIC_SERVER_URL=http://localhost:3000

SMTP_HOST=
SMTP_USER=
SMTP_PASS=
EMAIL_FROM=

# Optional: route adapter traffic (Instagram fetches) through an HTTP proxy.
# Format: http://user:password@host:port — targeting flags go in the password,
# e.g. FleetProxy residential: http://subuser:pass_country-us@residential.fleetproxy.io:12321
OUTBOUND_PROXY_URL=