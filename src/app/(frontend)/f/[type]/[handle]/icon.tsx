// The plain <link rel="icon"> counterpart to ./apple-icon — same rendering,
// so readers that look for either the touch icon or the favicon get the
// account's profile picture. Overrides the app-root icon.png for this route.
export { size, contentType } from './apple-icon'
export { default } from './apple-icon'
